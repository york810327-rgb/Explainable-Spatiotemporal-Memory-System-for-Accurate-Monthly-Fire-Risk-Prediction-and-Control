**2026年07月15日：项目启动、时空数据筑基与引擎兼容性调试**

实验人员： 蔡昀哲

工作内容：

1. 在本地电脑正式建立项目科研工作台，对学姐留下的原始数据进行首次本地读取。
2. 兼容性技术攻坚： 本地在调用 pyarrow 读取原始 .parquet 文件时，遭遇了 Repetition level histogram size mismatch 底层校验报错。经排查，此为 PyArrow 19.0.0 与原始数据存储格式的类型校验兼容冲突。通过修改读取引擎为 fastparquet，完美绕过底层校验，成功在本地无缝读入数据。
3. 安全性与防泄漏审计： 队长亲自对学姐发来的纯净数据集进行红线特征合规性审计，核对并阻断了所有可能导致“未来信息泄漏”的火灾历史及过火面积指标（如 burned\_area\_m2, burned\_frac 等）。经审计，确认学姐发来的 step7v3\_2\_P1\_time\_holdout\_lead1F1.parquet 已做好了前置硬拦截，数据源极度安全。

**2026年07月16日：P1协议时间外推切分与地理空间审计**

实验人员： 蔡昀哲、任艺杰

工作内容：

1. 时间外推切分（蔡昀哲）： 拒绝随机乱分，严格执行大创方案中的 P1时间外推协议。针对数据集中未直接暴露 year 列的硬报错（KeyError: 'year'），蔡昀哲在本地脚本中编写了时间字段转换机制，将 date 字段转化为 Pandas 时间戳并动态提取年份。成功将数据集隔离为：

   * 训练集（2015–2021年）：用于模型拟合。
   * 验证集（2022年）：用于模型评估与概率校准。
   * 导出最终安全的划分数据表 df\_clean\_local.parquet。
2. 空间底座审计（任艺杰）： 任艺杰在本地同步完成了地理空间主键和空间阻断字段的盘点。统计得出，全国样本中包含 2,630 个 独立的 cell\_id（网格像元）；同时完成了对生态区（BIOME）分布数据以及 50km/100km 多尺度阻断网格的盘点，编写并提交了 spatial\_distribution.txt 以及 spatial\_merge\_rules.md。

**2026年07月17日：一阶段基线回归模型实验完成与指标合流**

实验人员： 蔡昀哲、周逸凡、刘莹

工作内容：

1. 服务器数据合并： 蔡昀哲部署了配有 fastparquet 引擎的 Python 运行环境。通过蔡昀哲和任艺杰提供的关联逻辑，以 cell\_id 和 date 为双主键，将划分表与含有所有物理特征的特征大表 lead1\_F1\_wgrid50\_100.parquet 进行了无泄漏左连接。
2. 模型拟合与指标评测： 蔡昀哲正式运行了 run\_baseline.py。模型管线配置为：中位数缺失值填补 -> Z-score 标准化 -> 正则化逻辑回归（Logistic-L2, C=1.0）。
3. 实际测评： 融合了蔡昀哲在 evaluation.py 中写好的核心算分算法，模型在 2022 年验证集上做预测，成功输出并保存了实验指标汇总表 logistic\_l2\_metrics(1).csv。主要评测指标如下：

   * AUPRC (精准率-召回率曲线面积)： 0.209984
   * ROC-AUC (受试者工作特征曲线面积)： 0.9155592
   * Brier Score (概率预测均方误差)： 0.004374

**2026年07月18日：工程目录规范化与评估汇总模块构建**

实验人员： 蔡昀哲

工作内容：

1. 目录结构规范化：确立项目四大标准核心文件夹体系，即 data（存放样本与划分大表）、src（存放源代码）、results（存放战报表与模型权重）和 docs（存放文档资产）。统一修正代码中的绝对路径格式，消除底层路径逻辑冲突。
2. 自动化汇总开发：编写 metrics_summary.py，实现多模型、多协议评测指标表（如 baseline 结果及后续树模型结果）的自动读取与上下拼接，支持一键输出全模型性能测评总表。
3. 权重消融接口预留：针对方案中的“模块三：极度不平衡数据的权重策略消融”，独立编写了 weight_strategy.py 算法接口，预置了传统类别平衡（cw_balanced）与空间抽样设计权重（sw_dweight）方案。

**2026年07月19日：P3多尺度空间阻断切分与底座准备**

实验人员： 蔡昀哲、任艺杰

工作内容：

1. 空间泛化极限测试准备（蔡昀哲）：针对大尺度空间异质性问题，本地运行 p3_split.py，顺利完成多尺度的 P3 数据切分，成功生成 p3_split_grid10km.parquet、50km及100km的数据集并归档至 data 文件夹。
2. 特征工程链路核对（任艺杰）：协助核对 lead1_F1_wgrid50_100.parquet 大表中的物理特征集完整性，明确后续模块五（大尺度空间异质性分析）所需的 BIOME 落图所需依赖属性，为后续多尺度模型评估做前置准备。

**2026年07月20日：物理特征组消融方案确立与权重数据集生成**

实验人员： 周逸凡

工作内容：

1. 特征组消融方案敲定：严格依照《大创实验方案》模块四要求，确立采用“做减法”的 Drop 方案（方案A）。利用 03_feature_ablation.py，分别构建去除火历史（w/o Fire History）、去除干旱水分（w/o Drought/Moisture）及去除植被（w/o Vegetation）三组特征消融列表。
2. P3 消融测试数据生成：编写并运行 04_prepare_p3_weight_dataset.py，整合 84 维 full feature、空间 block 信息及权重参数，成功生成核心的 p3_weight_dataset.parquet，为后续模型对比提供数据底座。

**2026年07月21日：LightGBM主干模型极限测试、冲突修复与项目开源化**

实验人员： 蔡昀哲、刘莹、任艺杰、周逸凡

工作内容：

1. LightGBM非线性突破与深度打磨（蔡昀哲）：在 src 下运行新编写的 run_lgbm.py，涵盖 P1 及 P3 极限阻断管线。为防止模型死记硬背局部微气候致使 P3 测试崩溃，强制设置参数 max_depth=5，并硬拦截所有空间ID列混入特征。
2. 数据合并冲突修复（蔡昀哲）：运行模型时触发 Pandas 表 Merge 经典冲突（KeyError: 'y'），因划分表与特征大表中重叠了 y、BIOME 等列导致产生 y_x 后缀。通过补充重叠列剔除逻辑（overlap drop）完美修复。
3. 战报产出与推断分发（团队）：蔡昀哲成功生成 8 份核心产物（4个 .csv 成绩单交由队长自动汇总，4个 .joblib 模型权重发群）。针对任艺杰模块五、模块七所需的网格预测概率需求，敲定补救方案：由任艺杰/刘莹后续直接通过 joblib.load() 加载最优模型资产，调用 predict_proba() 自行提取。
4. 路径智能转化与 GitHub 迁移（蔡昀哲）：编写 auto_fix_paths.py 将绝对路径（D:/SCI/）全部智能替换为相对路径（./）。成功配置 .gitignore 清除 git 缓存机制中的误传大表（超过 100MB 的 .parquet），并在根目录编撰专业级 README.md 后，成功将核心代码推送至云端 GitHub 仓库。

**2026年07月22日：全面启用GitHub Flow分支协同与核心模块并行开发**

实验人员： 蔡昀哲、周逸凡、刘莹、任艺杰

工作内容：

1. 云端项目架构与分支基建（蔡昀哲）： 为提升团队协同开发效率并彻底隔离代码冲突，队长蔡昀哲在 GitHub 网页端基于纯净的 main 主分支，为四大核心模块创建了专属的平行工作分支：feat-module3-weight（权重策略消融）、feat-module4-ablation（物理特征组消融）、feat-module5-spatial（大尺度空间异质性分析）以及 feat-module6-shap（模型可解释性分析），并在各分支的 src 文件夹下预置了对应的代码脚本模块（如 task5_biome_radar.py、task6_shap_analysis.py 等）。
2. 多模块任务下发与并行开发（团队全员）： 团队彻底告别“单线阻塞式”开发。全体队员通过在本地终端执行 git fetch 与 git checkout 分支名 同步云端架构，并在各自独立的“平行宇宙”中开展冲刺阶段的开发任务。蔡昀哲与陶然开展三组权重对比实验及图表绘制；周逸凡开展基于 3 组特征残缺表（剔除火历史/干旱/植被）的模型性能衰减测试；任艺杰开展基于 BIOME 标签的生态区大尺度地图处理；刘莹开始编写全局蜂腰图与偏依赖图代码。
3. PR合并审查与主干合流机制建立（蔡昀哲）： 确立了团队最高级别的代码合并审查（Pull Request, PR）红线。当队员分支开发完成并发起合并请求时，队长在 GitHub 网页端的 Files changed 页面严格执行“两项安检”：排查是否误传超过 100MB 的数据大表（.parquet 或 .csv），以及是否违规硬编码绝对路径。审核无误后，通过 Merge pull request 将各模块心血汇入主干，随后执行 Delete branch 清理历史分支，并广播全员通过 git pull origin main 同步最新且 100% 可运行的官方底座代码。

**2026年07月23日：模块三权重消融代码开发与数据底座修复**

实验人员： 蔡昀哲

工作内容：

1. **极度不平衡数据的权重策略管线搭建（蔡昀哲）：** 蔡昀哲在 `feat-module3-weight` 分支中编写了 `task3_weight_strategy.py` 跑批管线，并调用了蔡昀哲提前封装好的 `weight_strategy.py` 底层接口（配方）。管线设计为固定使用 LightGBM 模型，遍历对比基准（cw_none__sw_none）、传统平衡法（cw_balanced__sw_none）以及空间抽样设计权重（cw_none__sw_dweight）3 组策略。
2. **底层数据读取错误排查与修复（蔡昀哲）：** 在初次跑批时，由于终端工作目录路径基准不一致以及误用了中期过渡表（`df_clean_local.parquet`，缺失 `year` 和权重列），导致连续出现 `FileNotFoundError` 与 `KeyError`。蔡昀哲迅速介入排查，将数据源切换为专为模块三、四定制的终极底座 `data/p3_weight_dataset.parquet`，成功使所有特征与标签完美咬合，跑通了实验管线。

**2026年07月24日：模块三实验结果落图与云端防泄漏合流**

实验人员： 蔡昀哲

工作内容：

1. **权重策略消融实验成绩单生成：** 蔡昀哲正式完成模块三的 3 组权重策略跑批实验。成功提取 2022 年验证集的预测概率，计算并输出了包含 AUPRC 与 Brier Score 这两项核心指标的战报文件 `03_weight_ablation_metrics.csv`，并安全沉淀至 `results` 目录。
2. **高阶指标对比制图与结论印证：** 蔡昀哲编写并运行了 `plot_weight_ablation.py`，利用 Seaborn 绘制了高质量的 1行2列 学术对比柱状图（`03_weight_strategy_comparison.png`）。可视化结果直观且强有力地印证了团队设计的“空间抽样设计权重法（sw_dweight）”在应对火灾稀有事件时，相较于传统的算法强制平衡，能够有效解决概率失真与假阳性偏高问题，实现了概率的精准纠偏（显著降低 Brier Score）。
3. **总控台防泄露审查与主干合流：** 蔡昀哲作为总控，在 GitHub 网页端对提交的 PR 进行了严格的防泄漏代码审查，确保没有将 `data/` 下的数据大表误传至云端。审核无误后，成功将 `task3_weight_strategy.py` 与 `plot_weight_ablation.py` 合流至 `main` 主分支，标志着模块三从模型训练到可视化的全链路完美闭环。

**2026年7月27日-2026年7月29日：核心图表收官、特征消融模型纠偏与数据底座冲突解决**

实验人员：蔡昀哲、刘莹、周逸凡、任艺杰 

工作内容：

1. **多模块作图收官与非线性机制归因（刘莹）**：在底层主干模型全部跑完的基础上，刘莹开始执行最终的指标合并与解释性图表收官。负责画出树模型与基线的对比图及 P3 多尺度泛化衰减图，并输出 SHAP 全局蜂腰图和偏依赖图。针对模块五的异质性分析要求，修改现有的 task6_shap_analysis.py代码逻辑，增加按 BIOME（生态区）分组的处理以绘制对比雷达图；最终结合精修出炉的图表，全面梳理并撰写非线性触发机制的生态学归因小结，为后续论文的 Discussion 部分奠定核心底稿。
2. **消融实验排查与树模型纠偏（周逸凡、蔡昀哲）**：周逸凡在运行特征消融时发现剔除植被特征后模型性能不降反升的异常现象。经队长排查判定，问题并非出在特征定义上（官方植被组已完美融合了长期静态结构与短期动态脱水特征），而是误用了对多重共线性敏感的线性基线模型（Logistic-L2）。队长紧急纠偏，指令严格对齐 03_feature_ablation.py
的官方特征分组名单，并切回 run_lgbm.py使用主力树模型 LightGBM 配配 cw_none__sw_dweight 权重策略重跑衰减测试，成功捕捉真实的非线性物理贡献。
3. **数据底座版本管控与规范化协同（蔡昀哲、周逸凡、任艺杰）**：团队内部暴露出下游盲区分析私自拼表导致网格数据错位（38个突变为460个）的严重事故。队长亲自对 04_prepare_p3_weight_dataset.py进行底层逻辑复盘，证实周逸凡生成的 p3_weight_dataset.parquet 训练底座完全合规（已自动保留受保护的空间坐标列）。队长果断收回数据控制权，叫停了私下传递残缺版和强行缝合表的操作，确立了该文件作为模型训练和基础作图的唯一标准；并规范指导任艺杰如需 valid_core 质控字段应直接调用上一级全量特征大表，从根本上杜绝了论文指标自相矛盾的致命隐患。

**2026年07月30日-2026年07月31日：预警盲区网格终极锁定、特征消融管线合流与复现阶段圆满收官**

实验人员： 蔡昀哲、周逸凡、任艺杰、刘莹

工作内容：

1. **模块七空间盲区与落图底座终极锁定（任艺杰、蔡昀哲）**：彻底攻克前期盲区分析中的网格数量虚高（460个笛卡尔积错位）与数据丢包（38个）事故。任艺杰直接调用全量特征大表 `lead1_F1_wgrid50_100.parquet`，通过 `["cell_id", "date"]` 双主键对齐 Left Join 安全合并 `valid_core` 质控字段。在 P3 100km 极限空间阻断下，成功且精准地筛选锁定 **40 个官方标准预警盲区网格**（导出 `p3_100km_blind_spots_official.csv`）。盲区在温带草原（BIOME 8，13个）、热带亚热带阔叶林（BIOME 1，9个）、高山草甸（BIOME 10，8个）等生态区的分布完全符合中国火灾空间异质性特征，为大尺度落图与 SHAP 瀑布图案例分析打下了绝对干净无误的数据底座。
2. **物理特征消融实验代码合流与机制释疑（周逸凡、蔡昀哲）**：周逸凡完成 `03_feature_ablation.py` 与 `05_lgbm_feature_ablation.py` 的代码重构并提交 PR，固定采用主力树模型 `LightGBM`（锁定 `max_depth=5` 防空间死记硬背）与 `cw_none__sw_dweight` 空间抽样设计权重策略。进度确认：剔除植被特征组（No Vegetation）后 AUPRC 出现合理下降（-0.002），彻底根治了早期 Logistic-L2 模型的多重共线性反常；剔除火历史特征组（No Fire History）后 AUPRC 发生了-0.068 的断崖式下跌，再次强有力地印证了“时空记忆”的核心主导地位；深度解释了剔除干旱组（No Drought）后 AUPRC 微升（+0.008）系受限树深度下稀释气象高频噪声及植被状态作为干旱“综合记忆代理”的生态学机制。
3. **基础复现全线收官与分组双轨拓展规划（蔡昀哲、团队全员）**：队长蔡昀哲主持全管线静态代码审查（Code Review），重点对数据防泄漏硬拦截（`audit.py`、`step4_final_qc.py`）和 P3 空间阻断断言（`step7_make_splits.py`）进行逻辑复核，确认《大创实验方案》的七大模块复现任务已全线圆满通关。确定项目正式进入“降速精雕”阶段，并确立后期“分组双轨制”拓展策略：Group 1（蔡昀哲、陶然、任艺杰）主攻“灾后生态恢复评估”拓展方向，开始预研基于斑块（Patch-based）框架的恢复时间与驱动机制建模，准备在系统上做二次开发。

**2026年08月03日-2026年08月04日：代码相对路径标准化、图表顶刊级视觉重构与消融成绩单 LaTeX 归档**

实验人员： 蔡昀哲、周逸凡、刘莹、任艺杰

工作内容：

1. **解释性与基线图表视觉精修及 PR 规范化（刘莹、蔡昀哲）**：刘莹对模型可解释性核心脚本 `task6_shap_analysis.py` 进行彻底重构，将代码中所有绝对路径智能转化为安全的相对路径 `./`，清理无用注释并成功提交代码合流 PR。对论文插图进行顶刊出版级（300 DPI）的视觉润色与微调。完成了Tree vs Baselines 性能对比图（`02_p1_tree_vs_baselines.png`）、P3 多尺度性能衰减图（`03_p3_multiscale_degradation.png`）和 BIOME 生态区 SHAP 极地雷达图（`06_biome_shap_radar.png`）的色彩、字号与图例重构，图表视觉表现力已完全达到 Top 级遥感与环境期刊发表标准。
2. **消融成绩单标准化归档与机制撰写（周逸凡）**：周逸凡对 `results/lgbm_feature_ablation_results.csv` 的最终实验数据（AUPRC, ROC-AUC, Brier Score）进行严密核对与格式化转换，整理为适用于 Overleaf 的标准 LaTeX 与 Markdown 表格。执笔撰写了消融实验结果机制分析的学术初稿：深度阐明了“剔除火历史特征后 AUPRC 断崖式暴跌 -0.068”对我们“时空记忆”核心论点的支撑作用，并从受限树深度下稀释气象噪声以及植被状态作为干旱“综合记忆代理”的生态学机制两个维度，合理解释了“剔除干旱特征后 AUPRC 微升 +0.008”的反直觉现象。
3. **全国空间预警盲区落图与典型案例初筛（任艺杰）**：任艺杰基于此前我们锁定的40 个官方标准盲区网格（`p3_100km_blind_spots_official.csv`），在本地利用经纬度坐标进行大尺度空间地图投影，绘制输出 `07_p3_100km_blind_spots_map.png`。从这 40 个网格中初筛出 2 个最具生态代表性的典型漏报/预警网格（包含 1 个温带草原 BIOME 8 网格、1 个针叶/阔叶林交错区 BIOME 1 网格），为绘制 SHAP 瀑布图案例分析打下了坚实的数据底座。
4. **模型资产与数据防泄漏合规审计（蔡昀哲）**：蔡昀哲对 `results/` 目录下保存的所有 `.joblib` 格式主力模型权重（如 `p1_lgbm_strict_model.joblib`）进行反序列化（`joblib.load()`）与预测 Parquet 数据的损坏性完整性校验，确认所有核心资产无损。协同队长蔡昀哲交叉复核 `audit.py` 和 `step4_final_qc.py` 源码，对 processed Parquet 数据流执行静态审计，再次确认特征集中无 `burned_area_m2` 或 `valid_weight` 等未来信息及标签代理残留，学术硬红线防控万无一失。

**2026年08月05日-2026年08月07日：微观案例瀑布图解析、基础复现 100% 完美收官**

实验人员： 蔡昀哲、任艺杰、周逸凡、刘莹

工作内容：

1. **时空预警微观案例瀑布图绘制与地理机理解析（任艺杰、蔡昀哲）**：任艺杰在本地调用 `shap.waterfall_plot()` 针对初筛出的 2 个典型盲区网格成功绘制出对应的 SHAP 瀑布图（Waterfall Plot）。结合网格在特定起火月份的具体气象（高饱和汽压差 VPD 胁迫、降水严重亏缺）与植被（低 NDVI 导致的脱水状态）特征数值，撰写了 300 字的林火地理机制常识印证小结。在微观网格尺度闭合解释了模型成功预警与漏报的物理成因，宣告模块七（空间落图与案例印证）全面通关。
2. **物理非线性归因小结与特征准入终极复盘（周逸凡、刘莹）**：刘莹结合全局 SHAP 蜂腰图（`04_shap_beeswarm.png`）和偏依赖曲线，撰写完成了《火灾非线性触发机制生态学归因小结》。重点分析了 VPD 和距离上次火灾月份（`months_since_last_fire`）的非线性“双重阈值效应”，深入揭示了林火发生的非线性物理临界突变机制。周逸凡对 `step5.5_build_featuresets1.py` 的特征准入逻辑进行终审，重点确认多尺度空间块 ID（Block-only）和抽样设计权重等辅助元数据未混入 predictors，确保特征选择流程无暇。
3. **《大创实验方案》七大模块基础复现全线收官（蔡昀哲、团队全员）**：队长蔡昀哲主持基础复现阶段的全局项目验收。**至 2026 年 8 月 7 日，学姐《大创实验方案》中的七大核心模块实验复现、评估总表汇总与可解释性归因工作已宣告 100% 完美收官。** 
